<% if event.bookmarked_by?(current_user) %>
  <%= link_to "ブックマークを外す", event_bookmarks_path(event), method: :delete,remote: true,class: "btn-info h3 text-white" %>
<% else %>
  <%= link_to "ブックマーク", event_bookmarks_path(event), method: :post ,remote: true,class: "btn-info h3 text-white" %>
<% end %>